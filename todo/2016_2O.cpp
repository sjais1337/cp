#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  freopen("diamond.in", "r", stdin);
  freopen("diamond.out", "w", stdout);

  int n; 
  ll K;
  cin >> n >> K;

  vector<ll> S(n);

  for(int i = 0; i < n ;i++){
    cin >> S[i];
  }

  sort(S.begin(), S.end());

  vector<ll> b_i(n);
  vector<ll> a_i(n);




  for(int i = 0; i < n; i++){

  }
}
